{
  "name": "paygate-vercel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "test:auth": "node scripts/phase2-auth-smoke.mjs",
    "test:registry": "node scripts/phase3-registry-smoke.mjs",
    "test:upstream": "node scripts/phase4-upstream-smoke.mjs",
    "test:proxy-unpaid": "node scripts/phase5-proxy-smoke.mjs",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@stellar/mpp": "^0.6.0",
    "@stellar/stellar-sdk": "^15.0.0",
    "@supabase/supabase-js": "^2.107.0",
    "express": "^5.2.1",
    "mppx": "^0.6.28",
    "zod": "^3.22.0"
  }
}
